---
import { formatDate } from "@/lib/utils";

interface Props {
  lastUpdated: Date | null;
}

const { lastUpdated } = Astro.props;
---

<footer class="border-t mt-8 p-4 mb-12">
  <div class="flex justify-between">
    <a href="/" class="text-sm text-muted-foreground hover:text-foreground transition-colors"> ← home </a>
    {lastUpdated && <span class="text-sm text-muted-foreground">Last updated: {formatDate(lastUpdated)}</span>}
  </div>
</footer>
